export interface SelectedSubCategory {
  id: number;
  dataKey: CryptoKey;
  dataKeyVersion: Date;
  name: string;
}

export interface SelectedFile {
  id: number;
  dataKey: CryptoKey;
  dataKeyVersion: Date;
  name: string;
}
